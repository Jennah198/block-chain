# Storage Arrays and Push
* **State Variables**: Dynamic arrays declared outside of functions are stored in `storage`.
* **Push Method**: The `.push()` member function adds a new element to the end of a storage array.
* **Dynamic Length**: The `.length` property automatically increases whenever an element is pushed.
* **Filtering Logic**: Even numbers are identified using `number % 2 == 0`.