Input Validation: Using require statements allows us to check conditions before executing logic. If the condition is false, the transaction reverts.

Internal Function Access: Functions marked as public (instead of external) can be called by both outside users and other functions within the same contract.

State Integrity: Preventing duplicate votes is a fundamental security practice in governance contracts to ensure fairness.
