Gas Efficiency: Mappings use a hash function to find the storage slot of a value instantly, avoiding the expensive "iteration" (looping) required by arrays.O(1) vs O(n): Mapping lookups are $O(1)$ (constant time), whereas array searches are $O(n)$ (linear time), meaning arrays get slower as they get larger.Clean Code: Mappings reduce boilerplate code by removing the need for complex loop logic.